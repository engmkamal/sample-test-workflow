import { WorkshopProposalList } from './workshop-proposal-list';

describe('WorkshopProposalList', () => {
  it('should create an instance', () => {
    expect(new WorkshopProposalList()).toBeTruthy();
  });
});
